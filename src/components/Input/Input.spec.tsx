// Libs
import { render } from '@testing-library/react';
// Self
import { Input } from './Input';

describe('Input element', () => {
  test('Input renders', () => {
    // render(<Input buttonColor={'green'} onClick={jest.fn()} />);
  });
});
